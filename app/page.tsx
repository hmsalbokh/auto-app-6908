'use client'; import { useTodos } from '../hooks/useTodos'; import TodoForm from '../components/TodoForm'; import TodoItem from '../components/TodoItem'; import { AnimatePresence } from 'framer-motion'; export default function Home() { const { todos, addTodo, toggleTodo, deleteTodo, isLoaded } = useTodos(); if (!isLoaded) return null; return ( <main className=\"min-h-screen py-12 px-4 bg-gradient-to-br from-slate-50 to-blue-50\"> <div className=\"max-w-2xl mx-auto\"> <header className=\"text-center mb-12\"> <h1 className=\"text-4xl font-bold text-gray-900 mb-2\">قائمة المهام الذكية</h1> <p className=\"text-gray-500\">نظم يومك بكل سهولة وأناقة</p> </header> <TodoForm onAdd={addTodo} /> <div className=\"space-y-2\"> <AnimatePresence mode='popLayout'> {todos.map(todo => ( <TodoItem key={todo.id} todo={todo} onToggle={toggleTodo} onDelete={deleteTodo} /> ))} </AnimatePresence> {todos.length === 0 && ( <div className=\"text-center py-12 text-gray-400\">لا توجد مهام حالياً، ابدأ بإضافة واحدة!</div> )} </div> </div> </main> ); }