'use client'; import { Todo } from '../types/todo'; import { Trash2, CheckCircle, Circle } from 'lucide-react'; import { motion } from 'framer-motion'; import { cn } from '../lib/utils'; export default function TodoItem({ todo, onToggle, onDelete }: { todo: Todo, onToggle: (id: string) => void, onDelete: (id: string) => void }) { const priorityColors = { low: 'bg-green-100 text-green-700', medium: 'bg-yellow-100 text-yellow-700', high: 'bg-red-100 text-red-700' }; return ( <motion.div layout initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, scale: 0.9 }} className=\"flex items-center justify-between p-4 bg-white rounded-2xl shadow-sm border border-gray-50 mb-3\"> <div className=\"flex items-center gap-3\"> <button onClick={() => onToggle(todo.id)} className=\"text-primary\"> {todo.completed ? <CheckCircle className=\"fill-current\" /> : <Circle />} </button> <span className={cn('text-lg', todo.completed && 'line-through text-gray-400')}>{todo.text}</span> <span className={cn('text-xs px-2 py-1 rounded-full', priorityColors[todo.priority])}> {todo.priority} </span> </div> <button onClick={() => onDelete(todo.id)} className=\"text-gray-400 hover:text-secondary transition-colors\"> <Trash2 size={20} /> </button> </motion.div> ); }