'use client'; import { useState } from 'react'; import { Plus } from 'lucide-react'; import { Priority } from '../types/todo'; export default function TodoForm({ onAdd }: { onAdd: (text: string, p: Priority) => void }) { const [text, setText] = useState(''); const [priority, setPriority] = useState<Priority>('medium'); const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if (!text.trim()) return; onAdd(text, priority); setText(''); }; return ( <form onSubmit={handleSubmit} className=\"space-y-4 mb-8\"> <div className=\"flex gap-2\"> <input value={text} onChange={(e) => setText(e.target.value)} placeholder=\"ما الذي تنوي فعله؟\" className=\"flex-1 p-3 rounded-xl border border-gray-200 focus:ring-2 focus:ring-primary outline-none transition-all\" /> <button type=\"submit\" className=\"bg-primary text-white p-3 rounded-xl hover:bg-opacity-90 transition-all flex items-center gap-2\"> <Plus size={20} /> <span>إضافة</span> </button> </div> <div className=\"flex gap-4 justify-center\"> {(['low', 'medium', 'high'] as Priority[]).map((p) => ( <label key={p} className=\"flex items-center gap-1 cursor-pointer text-sm\"> <input type=\"radio\" name=\"priority\" checked={priority === p} onChange={() => setPriority(p)} className=\"accent-primary\" /> {p === 'low' ? 'منخفضة' : p === 'medium' ? 'متوسطة' : 'عالية'} </label> ))} </div> </form> ); }